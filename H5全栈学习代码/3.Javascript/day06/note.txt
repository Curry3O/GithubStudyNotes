构造函数
字面量
通过下标或索引去访问数组
['a','b',123][1]   //b
['a','b',123].length   //3
栈方法
pop() 取出最后一个，返回取出元素，改变原数组
push() ,追加到末尾，返回添加后的数组的长度，改变原数组
队列方法
shiift() 取出第一个返回取出元素
unshift() 追加到头部，返回添加后的数组的长度，改变原数组
排序方法
reverse() 反转排序，改变原数组
sort() 排序
升序
sort(function(a,b)){
	return a-b;
}

